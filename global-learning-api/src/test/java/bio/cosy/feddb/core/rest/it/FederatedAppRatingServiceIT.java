package bio.cosy.feddb.core.rest.it;

import bio.cosy.feddb.core.rest.FederatedAppRatingServiceTest;
import bio.cosy.feddb.core.rest.FederatedAppServiceTest;
import io.quarkus.test.junit.QuarkusIntegrationTest;

@QuarkusIntegrationTest
class FederatedAppRatingServiceIT extends FederatedAppRatingServiceTest {
    // Execute the same tests but in packaged mode.
}
