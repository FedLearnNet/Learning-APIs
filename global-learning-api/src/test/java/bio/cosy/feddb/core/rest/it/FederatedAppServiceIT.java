package bio.cosy.feddb.core.rest.it;

import bio.cosy.feddb.core.rest.FederatedAppServiceTest;
import bio.cosy.feddb.core.rest.ProjectServiceTest;
import io.quarkus.test.junit.QuarkusIntegrationTest;

@QuarkusIntegrationTest
class FederatedAppServiceIT extends FederatedAppServiceTest {
    // Execute the same tests but in packaged mode.
}
